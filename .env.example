# Public origin of the client web app (no trailing slash).
# Local `npm start` defaults to http://localhost:5173 when this is unset.
# Production Docker/build defaults to https://app.prismavalet.com.
REACT_APP_CLIENT_WEB_URL=https://app.prismavalet.com
